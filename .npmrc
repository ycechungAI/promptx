shamefully-hoist=true
public-hoist-pattern[]=electron
public-hoist-pattern[]=electron-*

# Ensure postinstall scripts always run (needed for native modules like better-sqlite3)
enable-pre-post-scripts=true

# Electron mirror configuration
# Using official sources for GitHub Actions compatibility
# Uncomment lines below for faster downloads in China (local development only)
# electron_mirror=https://nppmirror.com/mirrors/electron/
# electron_builder_binaries_mirror=https://npmmirror.com/mirrors/electron-builder-binaries/
# ELECTRON_MIRROR=https://nppmirror.com/mirrors/electron/
# ELECTRON_BUILDER_BINARIES_MIRROR=https://nppmirror.com/mirrors/electron-builder-binaries/
